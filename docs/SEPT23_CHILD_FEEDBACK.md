# September 23 child feedback

This batch implements agreed priorities 1–5 and 13:

- Teaching artwork occupies a constrained, zero-minimum-width frame. Full tile coordinates replace the former four-pixel inset; the SVG retains explicit clipping to prevent neighbouring illustrations from bleeding in. Images fit without stretching in portrait and landscape.
- Preserve all 165 recordings. Text containing the reported mispronounced word “gate” bypasses its recording and speaks the exact English homophone “gait” using the existing speech fallback. Displayed text remains “gate”; equal character counts preserve boundary offsets. No audio is generated. Physical iPad listening remains unverified.
- Loss choices show Easier on the left and Same on the right. At the three-heart minimum, both honest choices remain Same.
- Each opponent card displays its actual creature/variant name.
- Number-duel entry shows a creature, 60-second badge, goal and +1/−1 rule. Results show outcome, score, goal, XP and best. Detailed score explanation remains available to assistive technology. A first zero-point record no longer labels a loss as a victory. Scoring, timing, records and saved rounds are unchanged.
- Distinct crawling, walking, running, dragon-riding and flying line illustrations replace pace dots. Mode labels, selection behavior and Ride/Fly locks remain.

## Confirmed decisions for later batches

The user approved closer distractors with plausible misspellings and real-word alternatives. Multiple choice will replace typed multiplication answers; no parallel typed-answer mode is wanted. One shield is earned after three reading victories and a won number duel with no intervening loss; it absorbs the next otherwise damaging reading mistake, persists across reloads and cannot stack. These three changes are not part of this batch.
